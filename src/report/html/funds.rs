//! The Funds tab.
//!
//! Nothing to draw yet: the fund model carries three portfolio-wide targets
//! and no rows, and there is no holding data to draw against them until the
//! look-through model lands.

pub(super) fn table() -> String {
    String::new()
}
